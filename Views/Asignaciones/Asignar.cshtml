@model sistemagestionactivos.Models.Asignacion
@{ ViewData["Title"] = "Asignar Activo"; }

<h1>Asignar Activo</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Asignar">
            <input type="hidden" asp-for="ActivoId" />
            <div class="form-group">
                <label asp-for="UsuarioId" class="control-label">Asignar a Empleados</label>
                <select asp-for="UsuarioId" class="form-control" asp-items="ViewBag.UsuarioId"></select>
            </div>
            <div class="form-group">
                <label asp-for="FechaAsignacion" class="control-label"></label>
                <input asp-for="FechaAsignacion" class="form-control" readonly />
            </div>
            <br/>
            <div class="form-group">
                <input type="submit" value="Confirmar Asignación" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>